# Product OS MCP — Environment Variables
# Copy this file to .env.mcp and fill in your values.
# In Cursor, these are typically set via the MCP config's "env" block instead.

# ── Required ────────────────────────────────────────────────────────────
#
# CONVEX_SITE_URL  — Your Convex deployment SITE URL (*.convex.site, NOT *.convex.cloud).
#                    Find it in the Convex dashboard under Settings > URL & Deploy Key.
#                    Example: https://trustworthy-kangaroo-277.convex.site
#
# MCP_API_KEY      — A secret key used to authenticate MCP calls.
#                    Must also be set as an environment variable in the Convex dashboard
#                    (Settings > Environment Variables > MCP_API_KEY).
#                    Generate a strong random value, e.g.:
#                      node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# WORKSPACE_SLUG   — The workspace slug you want the MCP to operate on.
#                    Find it in the Portal URL: /ws-<slug>/...

CONVEX_SITE_URL=https://your-deployment.convex.site
MCP_API_KEY=your-secret-api-key
WORKSPACE_SLUG=your-workspace-slug

# ── Analytics (optional) ────────────────────────────────────────────────
#
# POSTHOG_MCP_KEY  — PostHog project API key (starts with phc_).
#                    Omit to disable tracking entirely.
#
# MCP_USER_ID      — Your user ID for analytics attribution.
#                    Falls back to your OS username if not set.

POSTHOG_MCP_KEY=
MCP_USER_ID=
